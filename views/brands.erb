<h2>All brands in our distribution system:</h2>
<% if @brands.any?() %>
  <ul>
    <% @brands.each() do |brand| %>
      <li><%= brand.name() %></li>
      <% end %>
  </ul>

<% else %>

    <p>There are no brands yet!</p>

<% end %>
